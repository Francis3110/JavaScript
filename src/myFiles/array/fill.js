const arr = [1,4,2];

console.log(arr.fill(2,4));
// eslint-disable-next-line spaced-comment
// Phương thức fill() điền (sửa đổi) tất cả các phần tử của một mảng từ
// một chỉ mục bắt đầu (số không mặc định) đến một chỉ mục kết thúc (độ dài mảng mặc định) với một giá trị tĩnh.
// Nó trả về mảng đã sửa đổi